import { AlertCircle, PackageOpen, RefreshCw } from 'lucide-react';

export function LoadingGrid({ count = 4 }: { count?: number }) { return <div className="grid grid-cols-2 gap-x-4 gap-y-10 md:grid-cols-4 md:gap-x-6">{Array.from({ length: count }).map((_, index) => <div key={index} className="animate-pulse"><div className="aspect-[.82] bg-muted" /><div className="mt-4 h-4 w-2/3 bg-muted" /><div className="mt-2 h-3 w-1/3 bg-muted" /></div>)}</div>; }
export function ErrorState({ onRetry }: { onRetry?: () => void }) { return <div className="flex flex-col items-center justify-center border border-border bg-card px-6 py-16 text-center"><AlertCircle className="mb-4 size-7 text-primary" strokeWidth={1.5} /><p className="font-display text-xl">تعذّر تحميل هذه الصفحة</p><p className="mt-2 text-sm text-muted-foreground">يبدو أن هناك لحظة هدوء مؤقتة. حاولي مرة أخرى.</p>{onRetry && <button type="button" data-testid="button-retry" onClick={onRetry} className="mt-6 flex items-center gap-2 border border-primary px-5 py-2 text-sm text-primary"><RefreshCw className="size-4" /> إعادة المحاولة</button>}</div>; }
export function EmptyState({ title = 'لا توجد نتائج بعد', body = 'جرّبي تغيير اختياراتك لاستكشاف المزيد.' }: { title?: string; body?: string }) { return <div className="flex flex-col items-center justify-center border border-dashed border-border px-6 py-20 text-center"><PackageOpen className="mb-5 size-8 text-primary" strokeWidth={1.2} /><p className="font-display text-2xl">{title}</p><p className="mt-2 text-sm text-muted-foreground">{body}</p></div>; }